{
    "global" : {
        "lang": "en",              # language (unused)
        "sender": "",              # check sender e-mail to ignore spamming
        "tempdir": "./",           # temporarly directory to save images
        "ack": true,               # send HTTP ack to SRMail
        "srmail":"http://..."      # SRMail URL
    },
    "http": {
        "host": "127.0.0.1",       # HTTP host and port to receive e-mails
        "port": 8000               # as JSON via SRMail
    },
    "diaspora": {                  # Diaspora Pod info
        "pod": "https://framasphere.org",
        "username": "johndoe",
        "password": ""
    }
}
